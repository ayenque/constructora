<h1>Editar Departamento</h1>

<%= form_for(@departamento, :url => departamento_path(:proyecto_id => @proyecto.id)) do |f| %>

	<%= render( :partial => "form", :locals => { :f => f }) %>

<% end %>

<%= link_to 'Mostrar', departamentos_path(@departamento,:proyecto_id => @proyecto.id), :class => "btn btn-warning" %>

<%= link_to 'Atrás', departamentos_path(@departamento,:proyecto_id => @proyecto.id), :class => "btn btn-warning" %>

