<p id="notice"><%= notice %></p>


    <section class="intro-single">
      <div class="container">
        <div class="row">
          <div class="col-md-12 col-lg-8">
            <div class="title-single-box">
              <h1 class="title-single">Listado de Proyectos</h1>
              <span class="color-text-a">...</span>
            </div>
          </div>
          <div class="col-md-12 col-lg-4">
            <nav aria-label="breadcrumb" class="breadcrumb-box d-flex justify-content-lg-end">
              <ol class="breadcrumb">
                <li class="breadcrumb-item">
                  <%= link_to 'Proyectos', proyectos_path, :class=> "btn btn-warning" %>
                </li>
                <li class="breadcrumb-item active" aria-current="page">
                  <%= link_to 'Nuevo Proyecto', new_proyecto_path, :class=> "btn btn-warning" %>
                </li>
              </ol>
            </nav>
          </div>
        </div>
      </div>
    </section>

<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripcion</th>
      <th>Ruta de imagen</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @proyectos.each do |proyecto| %>
      <tr>
        <td><%= proyecto.nombre %></td>
        <td><%= proyecto.descripcion %></td>
        <td><%= proyecto.avatar_identifier %></td>
        <td><%= link_to("Ver departamentos",  departamentos_path(:proyecto_id => proyecto.id), :class => "btn btn-outline-info btn-sm") %></td>
        <td><%= link_to 'Mostrar', proyecto %></td>
        <td><%= link_to 'Editar', edit_proyecto_path(proyecto) %></td>
        <td><%= link_to 'Eliminar', proyecto, method: :delete, data: { confirm: 'Está seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  


